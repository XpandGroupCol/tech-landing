import type { ImageMetadata } from 'astro';

export interface AcordeonData {
    title: string;
    img?: string;
    imgName: string;
    imgTitle: string;
    imgDescription: string;
    btnDetail: string;
}
